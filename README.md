# React App of Movies project

Use webpack and babel to setup a react project that render a sample of react project and is using eslint to catch linter errors

## Run

run with:

```bash
npm install && npm start
```

to build run:

```bash
npm run build
```